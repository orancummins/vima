"""Marker so ``ofin.sdk._vendor`` is importable.

In development this package is empty. ``cli/build.py`` populates it with copies
of the three Open Finance ``client.py`` files when assembling the portable
``ofin.pyz`` bundle.
"""
