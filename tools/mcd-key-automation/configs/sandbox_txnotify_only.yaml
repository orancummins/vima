environment: sandbox
organization: mastercard

login_url: https://developer.mastercard.com/account/log-in
dashboard_url_pattern: "**/dashboard**"
key_password: "foobar!!"

# Single-API config used to diagnose the Transaction Notifications wizard.
# Run with MCD_PAUSE_STEP3=1 to pause when Step 3 ("Additional credentials")
# appears so you can drive the browser manually.
projects:
  - name: txnotify
    description: Mastercard Transaction Notifications
    apis: [txnotify]
